{
  "name": "query-overview",
  "version": "0.1.3",
  "description": "Interactive bar chart for exploration of query data from clinical trials",
  "main": "./build/queryOverview.js",
  "author": "Rho, Inc.",
  "license": "MIT",
  "dependencies": {
    "d3": "~3",
    "webcharts": "~1.7"
  },
  "scripts": {
    "format": "prettier --write 'src/**/*.js'",
    "build": "npm run format && rollup -c",
    "watch": "npm run format && rollup -c -w"
  },
  "devDependencies": {
    "babel-cli": "^6.0.0",
    "babel-preset-es2015-rollup": "^1.1.1",
    "rollup": "^0.41.6",
    "rollup-plugin-babel": "^2.4.0",
    "rollup-watch": "~3.2.2",
    "prettier":"~1"
  }
}
